@model IEnumerable<Veterinaria.Models.Cliente>

@{
    ViewData["Title"] = "Gestión de Clientes";
}

<div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8 gap-4">
    <div>
        <h2 class="text-headline-lg font-headline-lg text-on-background">Gestión de Clientes</h2>
        <p class="text-body-md font-body-md text-on-surface-variant mt-1">Directorio completo de propietarios registrados en el sistema.</p>
    </div>
    <a asp-action="Create" class="bg-primary text-on-primary font-label-md text-label-md px-6 py-3 rounded-lg hover:bg-primary-container transition-colors duration-200 flex items-center gap-2 shadow-sm cursor-pointer">
        <span class="material-symbols-outlined text-sm">person_add</span>
        Registrar Nuevo Cliente
    </a>
</div>

<!-- Client List Table -->
<div class="bg-surface-container-lowest border border-outline-variant rounded-xl overflow-hidden shadow-sm">
    <div class="overflow-x-auto">
        <table class="w-full text-left border-collapse">
            <thead>
                <tr class="bg-surface-container-low border-b border-outline-variant text-label-md font-label-md text-on-surface-variant uppercase tracking-wider">
                    <th class="px-6 py-4 font-semibold">Nombre</th>
                    <th class="px-6 py-4 font-semibold">Apellido</th>
                    <th class="px-6 py-4 font-semibold">DNI</th>
                    <th class="px-6 py-4 font-semibold">Teléfono</th>
                    <th class="px-6 py-4 font-semibold">Correo</th>
                    <th class="px-6 py-4 font-semibold text-right">Acciones</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-outline-variant text-body-md font-body-md">
                @foreach(var item in Model)
                {
                    <tr class="hover:bg-surface-container-lowest transition-colors cursor-pointer" onclick="window.location.href='@Url.Action("Details", "Cliente", new { id = item.IdCliente })'">
                        <td class="px-6 py-4 font-medium text-on-surface">@item.Nombre</td>
                        <td class="px-6 py-4 text-on-surface">@item.Apellido</td>
                        <td class="px-6 py-4 text-on-surface-variant">@item.DNI</td>
                        <td class="px-6 py-4 text-on-surface-variant">@item.Telefono</td>
                        <td class="px-6 py-4 text-on-surface-variant">@(item.Correo ?? "-")</td>
                        <td class="px-6 py-4 text-right space-x-2" onclick="event.stopPropagation();">
                            <a asp-action="Edit" asp-route-id="@item.IdCliente" class="text-on-surface-variant hover:text-on-surface p-1 rounded transition-colors inline-block" title="Editar">
                                <span class="material-symbols-outlined text-sm">edit</span>
                            </a>
                            <button type="button" onclick="openDeleteModal('@Url.Action("Delete", "Cliente", new { id = item.IdCliente })', '¿Eliminar cliente @item.Nombre @item.Apellido?');" class="text-error hover:text-on-error-container p-1 rounded transition-colors" title="Eliminar">
                                <span class="material-symbols-outlined text-sm">delete</span>
                            </button>
                        </td>
                    </tr>
                }
                
                @if(!Model.Any())
                {
                    <tr>
                        <td colspan="6" class="px-6 py-8 text-center text-on-surface-variant">No hay clientes registrados.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
